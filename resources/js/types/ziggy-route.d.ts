/**
 * Global `route()` helper injected by Ziggy's @routes blade directive.
 * Typed loosely so it satisfies both Link href props and .current() checks.
 */
declare function route(...args: unknown[]): any;
